export enum EventStatus {
  ARCHIVED = 'archived',
  DELETED = 'deleted',
  CLOSED = 'closed',
  OPENED = 'opened',
  WAITING = 'waiting'
}

export enum EventVisibility {
  LIMITED = 'limited',
  PUBLIC = 'public'
}
